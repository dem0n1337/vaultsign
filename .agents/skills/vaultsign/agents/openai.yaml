interface:
  display_name: "Vaultsign"
  short_description: "Repo-specific patterns and workflows for vaultsign"
  default_prompt: "Use the vaultsign repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true